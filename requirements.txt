# ============================================================
# Adaptive RAG – Requirements
# ============================================================
# 100% OFFLINE – No API keys.  No cloud services.  No env vars.
# After the one-time model downloads, every component runs
# entirely on your local machine without internet access.
# ============================================================

# PDF Processing
PyMuPDF>=1.23.0

# Text Splitting
langchain-text-splitters>=0.0.1

# Embeddings & Re-ranking (local models, downloaded once)
sentence-transformers>=2.2.2

# Vector Database (CPU-only, fully local)
faiss-cpu>=1.7.4

# Numerical
numpy>=1.24.0

# Local Ollama Communication
# NOTE: The 'requests' library is used ONLY to talk to
# Ollama running on localhost:11434 (your own machine).
# It does NOT contact any cloud API or external server.
requests>=2.28.0

# Web UI (runs on localhost:8501)
streamlit>=1.30.0

# Ablation Study & Evaluation
pandas>=2.0.0
matplotlib>=3.7.0

# Utilities (usually pre-installed, listed for completeness)
pickle5>=0.0.11; python_version < "3.8"
